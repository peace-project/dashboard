export default class RenderComponent {
    constructor(elementId, templateId, context) {
        this.elementId = elementId;
        this.templateId = templateId;
        this.context = context;
    }
}
